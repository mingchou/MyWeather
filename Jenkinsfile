node {
    /* def app */

    stage('Checkout') {
        checkout scm
	echo 'Checkout'
    }

    stage('Build') {
        /* app = docker.build("mingchou/MyWeather") */
	echo 'Build'
    }

    stage('Deploy') {
	echo 'Deploy'
    }
}
